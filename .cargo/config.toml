# Cargo workspace configuration
# NOTE: Do NOT set a default target here as it affects all builds including desktop
# Use explicit --target flag for specific platform builds instead

[env]
# Optimize release builds
CARGO_PROFILE_RELEASE_CODEGEN_UNITS = "1"
CARGO_PROFILE_RELEASE_LTO = "thin"
